# ANGPLE Widget Specification v1.0

> 위젯 개발자를 위한 공식 규약 문서
> SDK Corporation, 2026년 1월

---

## 목차

1. [위젯 철학과 원칙](#1-위젯-철학과-원칙)
2. [위젯 구조](#2-위젯-구조)
3. [widget.json 매니페스트 스펙](#3-widgetjson-매니페스트-스펙)
4. [위젯 렌더링](#4-위젯-렌더링)
5. [위젯 레이아웃 시스템](#5-위젯-레이아웃-시스템)
6. [Slot 시스템](#6-slot-시스템)
7. [관리자 UI](#7-관리자-ui)
8. [위젯 업로드 및 배포](#8-위젯-업로드-및-배포)
9. [위젯 개발 가이드](#9-위젯-개발-가이드)
10. [광고 위젯 통합](#10-광고-위젯-통합)

---

## 1. 위젯 철학과 원칙

### 1.1 위젯이란?

위젯은 **페이지에 삽입되는 독립적 UI 블록**입니다. WordPress의 위젯과 유사한 개념으로, 사이드바, 헤더, 푸터, 메인 콘텐츠 영역에 동적으로 배치할 수 있습니다.

### 1.2 플러그인과의 관계

```
┌─────────────────────────────────────────┐
│ 플러그인 (Plugin)                        │
│                                          │
│  - 백엔드 로직 (Go)                      │
│  - DB 테이블 / 마이그레이션              │
│  - API 엔드포인트                        │
│  - Hook 핸들러                           │
│  - 위젯 포함 가능 ← ─ ─ ┐               │
└──────────────────────────┼──────────────┘
                           │
┌──────────────────────────┼──────────────┐
│ 위젯 (Widget)            │               │
│                                          │
│  - 프론트엔드 UI 블록 (Svelte)           │
│  - 독립적으로 존재 가능                   │
│  - 또는 플러그인에 포함될 수 있음         │
│  - 설정을 통한 커스터마이징              │
└──────────────────────────────────────────┘
```

**핵심 구분:**

| 항목       | 플러그인            | 위젯                    |
| ---------- | ------------------- | ----------------------- |
| 주요 영역  | 백엔드 + 프론트엔드 | **프론트엔드만**        |
| 매니페스트 | `plugin.yaml`       | `widget.json`           |
| 독립성     | 완전 독립           | 독립 또는 플러그인 소속 |
| DB 접근    | 자체 테이블 가능    | API 호출만 가능         |
| Hook 등록  | 가능                | Slot 삽입만 가능        |

### 1.3 Core 위젯 vs 커스텀 위젯

| 구분      | Core 위젯                  | 커스텀 위젯                     |
| --------- | -------------------------- | ------------------------------- |
| 제공자    | ANGPLE 공식                | 서드파티 개발자 / 사이트 운영자 |
| 위치      | 소스코드에 내장 (하드코딩) | `custom-widgets/` 디렉토리      |
| 삭제      | 불가                       | 관리자가 삭제 가능              |
| 업데이트  | Core 업데이트 시 함께      | 독립적 업데이트                 |
| 보안 검증 | 코드 리뷰 완료             | ZIP 업로드 시 자동 검증         |

---

## 2. 위젯 구조

### 2.1 디렉토리 구조

```
custom-widgets/{widget-id}/
├── widget.json          # [필수] 위젯 매니페스트
├── index.svelte         # [필수] 메인 컴포넌트
├── settings-panel.svelte # [선택] 커스텀 설정 패널
├── screenshot.png       # [선택] 미리보기 이미지
├── README.md            # [권장] 사용법 문서
└── assets/              # [선택] 정적 리소스
    ├── icon.svg
    └── styles.css
```

### 2.2 네이밍 규칙

| 항목          | 규칙                 | 예시                               |
| ------------- | -------------------- | ---------------------------------- |
| 위젯 ID       | 소문자, 숫자, 하이픈 | `recent-posts`, `ad-banner-01`     |
| 디렉토리명    | 위젯 ID와 동일       | `custom-widgets/recent-posts/`     |
| 컴포넌트 파일 | kebab-case `.svelte` | `index.svelte`, `post-card.svelte` |
| 최소 ID 길이  | 3자                  | `ad` (불가) → `ad-slot` (가능)     |
| 최대 ID 길이  | 50자                 | -                                  |

---

## 3. widget.json 매니페스트 스펙

### 3.1 전체 스키마

```json
{
    "id": "my-widget",
    "name": "내 위젯",
    "version": "1.0.0",
    "description": "위젯 설명 (500자 이내)",
    "author": {
        "name": "개발자명",
        "email": "dev@example.com",
        "url": "https://example.com"
    },
    "category": "content",
    "slots": ["main", "sidebar"],
    "icon": "Star",
    "angpleVersion": ">=1.0.0",
    "screenshot": "screenshot.png",
    "main": "index.svelte",
    "allowMultiple": false,
    "sidebarOnly": false,
    "settings": {
        "title": {
            "label": "위젯 제목",
            "type": "text",
            "default": "최근 글",
            "description": "위젯 상단에 표시될 제목"
        },
        "count": {
            "label": "표시 개수",
            "type": "number",
            "default": 5,
            "min": 1,
            "max": 20
        },
        "showThumbnail": {
            "label": "썸네일 표시",
            "type": "boolean",
            "default": true
        }
    }
}
```

### 3.2 필수 필드

| 필드       | 타입     | 설명                                       |
| ---------- | -------- | ------------------------------------------ |
| `id`       | string   | 위젯 고유 식별자. `/^[a-z0-9-]+$/`, 3-50자 |
| `name`     | string   | 표시 이름 (1-100자)                        |
| `version`  | string   | 시맨틱 버전 (예: `1.0.0`)                  |
| `author`   | object   | 작성자 정보 (`name` 필수)                  |
| `category` | string   | 위젯 카테고리                              |
| `slots`    | string[] | 배치 가능한 슬롯 (1개 이상)                |

### 3.3 선택 필드

| 필드            | 타입    | 기본값         | 설명                             |
| --------------- | ------- | -------------- | -------------------------------- |
| `description`   | string  | -              | 위젯 설명 (500자 이내)           |
| `icon`          | string  | -              | Lucide 아이콘 이름               |
| `angpleVersion` | string  | -              | 호환 ANGPLE 버전                 |
| `screenshot`    | string  | -              | 스크린샷 파일 경로               |
| `main`          | string  | `index.svelte` | 메인 컴포넌트 파일               |
| `allowMultiple` | boolean | `false`        | 같은 페이지에 여러 인스턴스 허용 |
| `sidebarOnly`   | boolean | `false`        | 사이드바 전용                    |
| `settings`      | object  | -              | 설정 스키마 (자동 UI 생성용)     |

### 3.4 카테고리

| 카테고리  | 설명               | 예시                         |
| --------- | ------------------ | ---------------------------- |
| `content` | 콘텐츠 표시 위젯   | 추천 글, 최신 글, 인기 글    |
| `sidebar` | 사이드바 전용 위젯 | 카테고리 목록, 태그 클라우드 |
| `ad`      | 광고 위젯          | 배너 광고, 구글 애드센스     |
| `social`  | 소셜 위젯          | 실시간 채팅, 소셜 피드       |
| `utility` | 유틸리티 위젯      | 검색, 달력, 날씨             |
| `layout`  | 레이아웃 위젯      | 구분선, 여백, 컨테이너       |

### 3.5 슬롯

| 슬롯      | 한글명    | 설명                    |
| --------- | --------- | ----------------------- |
| `main`    | 메인 영역 | 페이지 중앙 콘텐츠 영역 |
| `sidebar` | 사이드바  | 좌/우 사이드바          |
| `header`  | 헤더      | 헤더 아래 영역          |
| `footer`  | 푸터      | 푸터 위 영역            |

### 3.6 설정 필드 타입

| 타입      | 옵션                            | UI 렌더링   |
| --------- | ------------------------------- | ----------- |
| `text`    | `default`, `description`        | 텍스트 입력 |
| `number`  | `default`, `min`, `max`, `step` | 숫자 입력   |
| `boolean` | `default`                       | 토글 스위치 |
| `color`   | `default`                       | 컬러 피커   |
| `select`  | `default`, `options[]`          | 드롭다운    |

**검증**: 모든 매니페스트는 Zod 스키마로 런타임 검증됩니다 (`WidgetManifestSchema`).

---

## 4. 위젯 렌더링

### 4.1 SSR 데이터 흐름

```
┌─────────────────────────────────────────────────────────────┐
│ Server (+layout.server.ts)                                  │
│                                                              │
│  1. settings.json에서 위젯 레이아웃 설정 조회                │
│     - 메인 영역 위젯 목록 + 순서                            │
│     - 사이드바 위젯 목록 + 순서                             │
│  2. 각 위젯의 설정값 조회                                    │
│  3. PageData로 클라이언트에 전달                             │
│                                                              │
│  return {                                                    │
│      widgetLayout: {                                         │
│          main: [                                             │
│              { id: 'recommended', type: 'recommended',       │
│                position: 0, enabled: true, settings: {} },   │
│              { id: 'new-board', type: 'new-board',          │
│                position: 1, enabled: true, settings: {} }    │
│          ],                                                  │
│          sidebar: [                                          │
│              { id: 'ad-1', type: 'ad', position: 0,         │
│                enabled: true, settings: { position: '...' }}│
│          ]                                                   │
│      }                                                       │
│  }                                                           │
└─────────────────────┬───────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────────┐
│ Client (+layout.svelte → WidgetEditor)                       │
│                                                              │
│  1. SSR 데이터로 위젯 목록 초기화                            │
│  2. 위젯 타입별 컴포넌트 매핑 (WIDGET_REGISTRY)              │
│  3. 순서대로 렌더링                                          │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 위젯 타입별 컴포넌트 매핑

현재 Core 위젯은 `WIDGET_REGISTRY`에 하드코딩되어 있습니다:

```typescript
// registry.ts
export const WIDGET_REGISTRY: Record<string, WidgetRegistryEntry> = {
    recommended: {
        name: '추천 글',
        icon: 'Star',
        description: '인기 있는 추천 게시글 목록',
        category: 'content',
        allowMultiple: false
    },
    'new-board': {
        name: '새로운 소식',
        icon: 'Newspaper',
        description: '최신 소식 게시판',
        category: 'content',
        allowMultiple: false
    },
    ad: {
        name: '광고',
        icon: 'Megaphone',
        description: '광고 슬롯',
        category: 'ad',
        allowMultiple: true,
        defaultSettings: {
            position: 'index-custom',
            height: '90px'
        }
    }
    // ... 추가 Core 위젯
};
```

### 4.3 동적 위젯 로딩 (Phase 11 목표)

향후 커스텀 위젯은 `custom-widgets/` 디렉토리에서 자동 스캔하여 동적으로 로드합니다:

```
1. 서버 시작 시 custom-widgets/ 스캔
2. 각 widget.json 파싱 및 Zod 검증
3. 유효한 위젯을 WIDGET_REGISTRY에 동적 등록
4. 클라이언트에서 동적 import로 컴포넌트 로드
```

---

## 5. 위젯 레이아웃 시스템

### 5.1 영역 구분

```
┌──────────────────────────────────────────────┐
│                  HEADER                       │
├────────────────────────────┬─────────────────┤
│                            │                  │
│      MAIN AREA             │    SIDEBAR       │
│                            │                  │
│  ┌──────────────────────┐  │  ┌────────────┐ │
│  │  Widget: 추천 글      │  │  │ Widget: 광고│ │
│  └──────────────────────┘  │  └────────────┘ │
│                            │                  │
│  ┌──────────────────────┐  │  ┌────────────┐ │
│  │  Widget: 새로운 소식  │  │  │ Widget: 태그│ │
│  └──────────────────────┘  │  └────────────┘ │
│                            │                  │
│  ┌──────────────────────┐  │                  │
│  │  Widget: 광고         │  │                  │
│  └──────────────────────┘  │                  │
│                            │                  │
├────────────────────────────┴─────────────────┤
│                  FOOTER                       │
└──────────────────────────────────────────────┘
```

### 5.2 WidgetConfig 타입

```typescript
interface WidgetConfig {
    /** 위젯 인스턴스 고유 ID (UUID) */
    id: string;

    /** 위젯 타입 (WIDGET_REGISTRY 키 또는 커스텀 위젯 ID) */
    type: string;

    /** 영역 내 순서 (0부터 시작) */
    position: number;

    /** 활성화 여부 */
    enabled: boolean;

    /** 위젯별 설정값 */
    settings: Record<string, unknown>;
}

interface WidgetLayout {
    main: WidgetConfig[];
    sidebar: WidgetConfig[];
}
```

### 5.3 드래그앤드롭 순서 변경

관리자 UI에서 위젯의 순서를 드래그앤드롭으로 변경할 수 있습니다:

```
1. 관리자가 위젯 카드를 드래그
2. position 값 재계산
3. API 호출: PUT /api/v2/admin/widget-layout
4. settings.json 또는 DB에 저장
```

### 5.4 레이아웃 저장/로드 API

```
GET    /api/v2/admin/widget-layout          # 현재 레이아웃 조회
PUT    /api/v2/admin/widget-layout          # 레이아웃 저장

# 요청 본문
{
    "main": [
        { "id": "uuid-1", "type": "recommended", "position": 0, "enabled": true, "settings": {} },
        { "id": "uuid-2", "type": "new-board", "position": 1, "enabled": true, "settings": {} }
    ],
    "sidebar": [
        { "id": "uuid-3", "type": "ad", "position": 0, "enabled": true, "settings": { "position": "sidebar-top" } }
    ]
}
```

---

## 6. Slot 시스템 (테마/플러그인 확장점)

### 6.1 Layout Slot vs Widget Slot

| 구분 | Layout Slot                        | Widget Slot                |
| ---- | ---------------------------------- | -------------------------- |
| 목적 | 테마/플러그인이 임의 컴포넌트 삽입 | 위젯 배치 영역             |
| 제어 | `SlotRegistry` API                 | `WidgetLayout` 설정        |
| 배치 | 코드로 등록                        | 관리자 UI에서 드래그앤드롭 |

### 6.2 사용 가능한 Layout Slot 목록

[Core 스펙 §9.3](../../angple-backend/docs/specs/core-spec-v1.0.md#93-slot-시스템)과 동일:

| 슬롯                   | 위치                 |
| ---------------------- | -------------------- |
| `header-before`        | 헤더 상단            |
| `header-after`         | 헤더 하단            |
| `sidebar-left-top`     | 왼쪽 사이드바 상단   |
| `sidebar-left-bottom`  | 왼쪽 사이드바 하단   |
| `sidebar-right-top`    | 오른쪽 사이드바 상단 |
| `sidebar-right-bottom` | 오른쪽 사이드바 하단 |
| `content-before`       | 메인 콘텐츠 상단     |
| `content-after`        | 메인 콘텐츠 하단     |
| `footer-before`        | 푸터 상단            |
| `footer-after`         | 푸터 하단            |
| `background`           | 배경 (테마용)        |
| `landing-hero`         | 랜딩 히어로 (테마용) |
| `landing-content`      | 랜딩 콘텐츠 (테마용) |

### 6.3 SlotRegistry API

```typescript
import {
    registerComponent,
    getComponentsForSlot,
    removeComponentsBySource,
    subscribeToSlotChanges
} from '$lib/components/slot-manager';

// 컴포넌트 등록
registerComponent(
    'sidebar-right-top', // 슬롯 이름
    MyComponent, // Svelte 컴포넌트
    10, // 우선순위 (낮을수록 먼저)
    { prop1: 'value' }, // props
    'my-plugin' // 소스 식별자
);

// 슬롯 변경 구독 (리액티브 업데이트)
const unsubscribe = subscribeToSlotChanges(() => {
    // 슬롯 변경 시 실행
});
```

### 6.4 우선순위 기반 컴포넌트 삽입

동일 슬롯에 여러 컴포넌트가 등록된 경우, **우선순위(priority)가 낮은 것이 먼저** 렌더링됩니다:

```
sidebar-right-top:
  1. [priority: 5]  광고 배너 (banner 플러그인)
  2. [priority: 10] 태그 클라우드 (테마)
  3. [priority: 50] 통계 위젯 (analytics 플러그인)
```

---

## 7. 관리자 UI

### 7.1 레이아웃 관리 탭

관리자 대시보드에서 위젯 레이아웃을 시각적으로 관리합니다:

```
┌─────────────────────────────────────────────────┐
│  위젯 관리                                        │
├──────────────────────────┬──────────────────────┤
│  메인 영역               │  사이드바             │
│                          │                       │
│  ┌────────────────────┐  │  ┌─────────────────┐ │
│  │ ≡ 추천 글     ⚙ ✕ │  │  │ ≡ 광고     ⚙ ✕ │ │
│  └────────────────────┘  │  └─────────────────┘ │
│  ┌────────────────────┐  │  ┌─────────────────┐ │
│  │ ≡ 새로운 소식 ⚙ ✕ │  │  │ ≡ 태그     ⚙ ✕ │ │
│  └────────────────────┘  │  └─────────────────┘ │
│                          │                       │
│  [+ 위젯 추가]           │  [+ 위젯 추가]       │
├──────────────────────────┴──────────────────────┤
│                [저장]  [초기화]                    │
└─────────────────────────────────────────────────┘
```

**기능:**

-   `≡`: 드래그 핸들 (순서 변경)
-   `⚙`: 위젯 설정 패널 열기
-   `✕`: 위젯 제거
-   `[+ 위젯 추가]`: 사용 가능한 위젯 목록에서 선택

### 7.2 설치된 위젯 탭

```
┌─────────────────────────────────────────────────┐
│  설치된 위젯                    [위젯 업로드 ↑]   │
├─────────────────────────────────────────────────┤
│                                                  │
│  Core 위젯 (삭제 불가)                            │
│  ┌──────────────────────────────────────┐       │
│  │ ★ 추천 글        v1.0.0  [content]  │       │
│  │ 📰 새로운 소식    v1.0.0  [content]  │       │
│  │ 📢 광고           v1.0.0  [ad]      │       │
│  └──────────────────────────────────────┘       │
│                                                  │
│  커스텀 위젯                                      │
│  ┌──────────────────────────────────────┐       │
│  │ 🔥 인기 글         v1.2.0  [content]  [삭제] │
│  │ 🌤 날씨 위젯       v1.0.0  [utility]  [삭제] │
│  └──────────────────────────────────────┘       │
│                                                  │
└─────────────────────────────────────────────────┘
```

### 7.3 위젯 설정 패널

위젯의 `settings` 스키마에 따라 자동으로 설정 UI가 생성됩니다:

```
┌─────────────────────────────┐
│  추천 글 위젯 설정            │
├─────────────────────────────┤
│                              │
│  위젯 제목                   │
│  ┌─────────────────────────┐│
│  │ 추천 글                  ││
│  └─────────────────────────┘│
│                              │
│  표시 개수                   │
│  ┌───────┐                  │
│  │ 5     │ (1-20)           │
│  └───────┘                  │
│                              │
│  썸네일 표시                 │
│  [■━━━━━━━] ON              │
│                              │
│        [저장]  [취소]         │
└─────────────────────────────┘
```

---

## 8. 위젯 업로드 및 배포

### 8.1 ZIP 업로드 프로세스

```
1. 관리자가 ZIP 파일 업로드
2. 서버에서 ZIP 검증:
   a. 파일 크기 제한 (10MB)
   b. 경로 탐색 공격 방지 (.., /, \ 체크)
   c. widget.json 존재 여부 확인
   d. widget.json Zod 스키마 검증
   e. main 컴포넌트 파일 존재 확인
3. custom-widgets/{widget-id}/ 에 압축 해제
4. 위젯 레지스트리에 등록
5. 관리자에게 성공/실패 응답
```

### 8.2 보안 검증

| 검증 항목   | 방법                                                                           | 실패 시        |
| ----------- | ------------------------------------------------------------------------------ | -------------- |
| ZIP 크기    | 10MB 이하                                                                      | 업로드 거부    |
| 경로 탐색   | `..`, `\`, 절대 경로, Null byte 차단                                           | 업로드 거부    |
| 매니페스트  | `widget.json` Zod 스키마 검증                                                  | 업로드 거부    |
| 메인 파일   | `main` 필드의 파일 존재 확인                                                   | 업로드 거부    |
| 파일 확장자 | `.svelte`, `.ts`, `.js`, `.css`, `.json`, `.md`, `.png`, `.jpg`, `.svg`만 허용 | 해당 파일 무시 |
| ID 충돌     | 기존 Core 위젯 ID와 중복 불가                                                  | 업로드 거부    |

### 8.3 공식 vs 커스텀 구분

| 구분      | 공식 (Core)    | 커스텀            |
| --------- | -------------- | ----------------- |
| 저장 위치 | 소스코드 내장  | `custom-widgets/` |
| 삭제      | 불가           | 관리자 삭제 가능  |
| 표시      | `[공식]` 배지  | `[커스텀]` 배지   |
| 신뢰도    | 코드 리뷰 완료 | 업로드자 책임     |

---

## 9. 위젯 개발 가이드

### 9.1 최소 위젯 예제 (Hello World)

**widget.json:**

```json
{
    "id": "hello-world",
    "name": "Hello World",
    "version": "1.0.0",
    "author": { "name": "Developer" },
    "category": "utility",
    "slots": ["main", "sidebar"]
}
```

**index.svelte:**

```svelte
<script lang="ts">
    // Svelte 5 Rune 모드 필수
    let { settings = {} }: { settings?: Record<string, unknown> } = $props();
</script>

<div class="bg-card rounded-lg border p-4">
    <h3 class="text-lg font-semibold">Hello World!</h3>
    <p class="text-muted-foreground">첫 번째 ANGPLE 위젯입니다.</p>
</div>
```

### 9.2 설정 가능한 위젯 예제

**widget.json:**

```json
{
    "id": "greeting-widget",
    "name": "인사 위젯",
    "version": "1.0.0",
    "author": { "name": "Developer" },
    "category": "content",
    "slots": ["main", "sidebar"],
    "settings": {
        "greeting": {
            "label": "인사말",
            "type": "text",
            "default": "안녕하세요!"
        },
        "backgroundColor": {
            "label": "배경색",
            "type": "color",
            "default": "#f0f9ff"
        },
        "showBorder": {
            "label": "테두리 표시",
            "type": "boolean",
            "default": true
        }
    }
}
```

**index.svelte:**

```svelte
<script lang="ts">
    let { settings = {} }: { settings?: Record<string, unknown> } = $props();

    let greeting = $derived((settings.greeting as string) ?? '안녕하세요!');
    let bgColor = $derived((settings.backgroundColor as string) ?? '#f0f9ff');
    let showBorder = $derived((settings.showBorder as boolean) ?? true);
</script>

<div class="rounded-lg p-4" class:border={showBorder} style:background-color={bgColor}>
    <p class="text-lg">{greeting}</p>
</div>
```

### 9.3 API 연동 위젯 예제

**widget.json:**

```json
{
    "id": "popular-posts",
    "name": "인기 게시글",
    "version": "1.0.0",
    "author": { "name": "Developer" },
    "category": "content",
    "slots": ["main", "sidebar"],
    "icon": "TrendingUp",
    "settings": {
        "count": {
            "label": "표시 개수",
            "type": "number",
            "default": 5,
            "min": 1,
            "max": 20
        },
        "period": {
            "label": "기간",
            "type": "select",
            "default": "week",
            "options": [
                { "label": "오늘", "value": "day" },
                { "label": "이번 주", "value": "week" },
                { "label": "이번 달", "value": "month" }
            ]
        }
    }
}
```

**index.svelte:**

```svelte
<script lang="ts">
    import { onMount } from 'svelte';

    let { settings = {} }: { settings?: Record<string, unknown> } = $props();

    let count = $derived((settings.count as number) ?? 5);
    let period = $derived((settings.period as string) ?? 'week');

    interface Post {
        id: number;
        title: string;
        view_count: number;
    }

    let posts = $state<Post[]>([]);
    let loading = $state(true);

    async function fetchPosts() {
        loading = true;
        try {
            const res = await fetch(`/api/v2/posts/popular?count=${count}&period=${period}`);
            const data = await res.json();
            if (data.success) {
                posts = data.data;
            }
        } catch (e) {
            console.error('인기 게시글 조회 실패:', e);
        } finally {
            loading = false;
        }
    }

    onMount(() => {
        fetchPosts();
    });

    // 설정 변경 시 재조회
    $effect(() => {
        count;
        period;
        fetchPosts();
    });
</script>

<div class="bg-card rounded-lg border p-4">
    <h3 class="mb-3 text-lg font-semibold">🔥 인기 게시글</h3>

    {#if loading}
        <div class="animate-pulse space-y-2">
            {#each Array(count) as _}
                <div class="bg-muted h-4 rounded"></div>
            {/each}
        </div>
    {:else}
        <ul class="space-y-2">
            {#each posts as post, i}
                <li class="flex items-center gap-2">
                    <span class="text-muted-foreground text-sm">{i + 1}</span>
                    <a href="/boards/free/{post.id}" class="truncate text-sm hover:underline">
                        {post.title}
                    </a>
                    <span class="text-muted-foreground ml-auto text-xs">
                        {post.view_count}
                    </span>
                </li>
            {/each}
        </ul>
    {/if}
</div>
```

---

## 10. 광고 위젯 통합

### 10.1 angple-ads 연동

광고 시스템은 별도 서비스(`angple-ads`)로 운영되며, 프론트엔드에서 `VITE_ADS_URL` 환경변수로 연결합니다:

```
VITE_ADS_URL=https://ads.damoang.net
```

### 10.2 AdSlot 컴포넌트 표준

```svelte
<script lang="ts">
    import { ADS_URL } from '$lib/config';

    let { position, height = '90px' }: { position: string; height?: string } = $props();
</script>

<div class="ad-slot" style:height style:min-height={height}>
    <iframe
        src="{ADS_URL}/serve?position={position}"
        width="100%"
        height="100%"
        frameborder="0"
        scrolling="no"
        loading="lazy"
        title="광고"
    ></iframe>
</div>

<style>
    .ad-slot {
        width: 100%;
        overflow: hidden;
    }
</style>
```

### 10.3 광고 위치(position) 규약

| Position         | 위치               | 크기    |
| ---------------- | ------------------ | ------- |
| `header-top`     | 헤더 최상단        | 728x90  |
| `header-bottom`  | 헤더 하단          | 728x90  |
| `sidebar-top`    | 사이드바 상단      | 300x250 |
| `sidebar-middle` | 사이드바 중간      | 300x250 |
| `sidebar-bottom` | 사이드바 하단      | 300x600 |
| `content-top`    | 콘텐츠 상단        | 728x90  |
| `content-middle` | 콘텐츠 중간        | 728x90  |
| `content-bottom` | 콘텐츠 하단        | 728x90  |
| `index-custom`   | 메인 페이지 커스텀 | 가변    |
| `post-before`    | 게시글 상단        | 728x90  |
| `post-after`     | 게시글 하단        | 728x90  |

> 광고 시스템의 상세 연동 방법은 [내부 연동 스펙](../../angple-backend/docs/specs/internal-integration-spec.md)을 참조하세요.

---

**— 문서 끝 —**
