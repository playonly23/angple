# Angple Frontend Environment Configuration

# Environment
NODE_ENV=development
APP_ENV=local

# SSR용 내부 API URL (서버 사이드에서만 사용)
# 로컬: http://localhost:8082/api/v2
# Docker: http://angple-api:8082/api/v2
INTERNAL_API_URL=http://localhost:8082/api/v2

# Recommended Data Path
# 로컬 개발: 비워두면 static 폴더의 JSON 파일 사용
# 프로덕션: 실제 경로 설정
RECOMMENDED_DATA_PATH=
# RECOMMENDED_DATA_PATH=/home/damoang/www/data/cache/recommended

# Legacy Integration (현세대 연동)
LEGACY_API_URL=http://localhost/api

# Legacy Site URL (하이브리드 운영 기간 동안 레거시 사이트 연동)
# 프로덕션: https://damoang.net
VITE_LEGACY_URL=https://damoang.net

# Legacy Ads URL (광고 서버)
# 프로덕션: https://ads.damoang.net
VITE_ADS_URL=https://ads.damoang.net

# Legacy S3 URL (S3 CDN)
# 프로덕션: https://s3.damoang.net
VITE_S3_URL=https://s3.damoang.net

# Mock Mode (백엔드 미준비 시 사용)
# true로 설정하면 실제 API 대신 Mock 데이터 사용
VITE_USE_MOCK=false

# ==============================================
# Docker Compose Configuration
# ==============================================

# Project name prefix for container names
# Container names: {COMPOSE_PROJECT_NAME}-web, {COMPOSE_PROJECT_NAME}-admin
COMPOSE_PROJECT_NAME=angple

# Port mappings (host:container)
WEB_PORT=3010
ADMIN_PORT=3011

# Data directory for persistent storage (settings.json, etc.)
DATA_PATH=./data

# ==============================================
# GitHub Packages Plugin Installation
# ==============================================

# 32바이트 hex 키 (토큰 암호화용)
# 생성: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
GITHUB_TOKEN_ENCRYPTION_KEY=

# Pre-built images (optional, leave empty to build locally)
# ANGPLE_WEB_IMAGE=ghcr.io/example/angple-web:latest
# ANGPLE_ADMIN_IMAGE=ghcr.io/example/angple-admin:latest
