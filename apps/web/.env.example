# 환경 변수 예제 파일
# 복사해서 사용: cp .env.example .env

# SSR용 내부 API URL (서버 사이드에서만 사용)
# 클라이언트(브라우저)는 항상 /api/v2 상대경로 사용 (nginx 프록시)
#
# 로컬 개발: http://localhost:8082/api/v2
# Docker:    http://angple-api:8082/api/v2
INTERNAL_API_URL=http://localhost:8082/api/v2
