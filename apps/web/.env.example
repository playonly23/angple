# ============================================
# Angple Web App - Environment Configuration
# ============================================
#
# 사용법:
#   1. 복사: cp .env.example .env.local
#   2. 값 수정 후 개발 서버 실행: pnpm dev
#
# 루트의 .env.local과 이 파일 모두 로드됩니다.
# 이 파일의 값이 루트 설정을 오버라이드합니다.
# ============================================

# --------------------------------------------
# 개발 서버 설정
# --------------------------------------------
# Vite 개발 서버 포트 (로컬 개발 시)
VITE_PORT=3010

# 허용 호스트 (쉼표로 구분)
VITE_ALLOWED_HOSTS=localhost,web.damoang.net,damoang.dev

# --------------------------------------------
# API 설정
# --------------------------------------------
# 클라이언트에서 사용하는 API URL
VITE_API_URL=http://localhost:8081/api/v2

# SSR 서버에서 사용하는 내부 API URL
INTERNAL_API_URL=http://localhost:8081/api/v2

# API 프록시 타겟 (Vite dev server)
VITE_API_PROXY_TARGET=http://localhost:8081

# --------------------------------------------
# 레거시 연동
# --------------------------------------------
VITE_LEGACY_URL=https://damoang.net
VITE_ADS_URL=https://ads.damoang.net
VITE_S3_URL=https://s3.damoang.net

# --------------------------------------------
# 기능 플래그
# --------------------------------------------
# Mock 모드 (true: API 없이 테스트 데이터 사용)
VITE_USE_MOCK=false

# SSR 활성화
VITE_SSR=true
