# ============================================
# Angple Web App - Environment Configuration
# ============================================
#
# 사용법:
#   1. 복사: cp .env.example .env.local
#   2. 값 수정 후 개발 서버 실행: pnpm dev
#
# 루트의 .env.local과 이 파일 모두 로드됩니다.
# 이 파일의 값이 루트 설정을 오버라이드합니다.
# ============================================

# --------------------------------------------
# 개발 서버 설정
# --------------------------------------------
# Vite 개발 서버 포트 (로컬 개발 시)
VITE_PORT=3010

# 허용 호스트 (쉼표로 구분)
VITE_ALLOWED_HOSTS=localhost,web.damoang.net,damoang.dev

# --------------------------------------------
# API 설정
# --------------------------------------------
# 클라이언트에서 사용하는 API URL
VITE_API_URL=http://localhost:8081/api/v2

# SSR 서버에서 사용하는 내부 API URL
INTERNAL_API_URL=http://localhost:8081/api/v2

# API 프록시 타겟 (Vite dev server)
VITE_API_PROXY_TARGET=http://localhost:8081

# 쿠키 도메인 (서브도메인 간 인증 공유, 예: ".example.com")
# Go 백엔드의 cookieDomain()과 일치시켜야 합니다
# 로컬 개발 시 비워두면 현재 호스트만 사용
COOKIE_DOMAIN=

# --------------------------------------------
# 레거시 연동
# --------------------------------------------
VITE_LEGACY_URL=
VITE_ADS_URL=
VITE_S3_URL=

# 레거시 SSO 쿠키명 (빈 값이면 레거시 SSO 비활성화)
VITE_LEGACY_SSO_COOKIE=

# --------------------------------------------
# GAM (Google Ad Manager)
# --------------------------------------------
VITE_GAM_NETWORK_CODE=
VITE_ADSENSE_CLIENT=
VITE_GAM_SITE_NAME=

# 사이트 표시명 (기본값: Angple)
VITE_SITE_NAME=

# --------------------------------------------
# 데이터베이스 (서버 사이드 전용)
# --------------------------------------------
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=angple

# --------------------------------------------
# 기능 플래그
# --------------------------------------------
# Mock 모드 (true: API 없이 테스트 데이터 사용)
VITE_USE_MOCK=false

# SSR 활성화
VITE_SSR=true

# --------------------------------------------
# 보안 (Cloudflare Turnstile)
# --------------------------------------------
# 클라이언트용 사이트 키 (공개)
PUBLIC_TURNSTILE_SITE_KEY=

# 서버용 시크릿 키 (비공개 — .env.local에 설정)
# TURNSTILE_SECRET_KEY=
